allprojects {
    repositories {
        google()
        mavenCentral()
        // 네이버 지도 SDK (flutter_naver_map) 아카이브 레포
        maven { url 'https://repository.map.naver.com/archive/maven' }
    }
}
