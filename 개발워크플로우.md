# ODD 앱 개발 워크플로우

## 코드 수정 후 폰에 반영하는 전체 흐름

---

### 1단계 — 코드 수정
- Cursor 또는 VS Code에서 `C:\Users\chahy\OneDrive\문서\GitHub\odd-app\lib\` 안 파일 수정

---

### 2단계 — APK 빌드 + 갤럭시 설치
터미널(PowerShell)에서:
```powershell
cd "C:\Users\chahy\OneDrive\문서\GitHub\odd-app"
.\install.ps1
```
> 약 2~3분 소요. 빌드 완료 후 자동으로 갤럭시(R39M30ERHSH)에 설치까지 완료됨.

---

### 3단계 — 갤럭시 화면을 PC에서 미러링
```powershell
scrcpy -s R39M30ERHSH
```
> USB로 연결된 갤럭시 화면이 PC 창으로 뜸. 마우스/키보드 조작 가능.

---

### 전체 한 번에 (수정 완료 후)
```powershell
cd "C:\Users\chahy\OneDrive\문서\GitHub\odd-app"
.\install.ps1
# 설치 완료 후
scrcpy -s R39M30ERHSH
```

---

### 참고 — 기기 연결 확인
```powershell
adb devices
# R39M30ERHSH    device 로 떠야 정상
```

### 참고 — scrcpy 옵션
| 옵션 | 설명 |
|------|------|
| `--max-fps 60` | 60fps로 부드럽게 |
| `--window-title "ODD 테스트"` | 창 제목 설정 |
| `--record screen.mp4` | 화면 녹화 |
| `--no-audio` | 소리 없이 미러링만 |
