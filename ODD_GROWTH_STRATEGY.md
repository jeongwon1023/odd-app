# ODD 성장 전략 & 실행 계획서
> "AI가 만든 코스"에서 "사람이 다녀온 코스"로
> Opus 4.8 분석 — 2026.06.29

---

## 핵심 진단 (코드가 증명하는 현실)

현재 1,944개 코스는 AI 생성이 아니라 **순열 조합기(combinatorial generator)**의 산물.
- 장소 풀에서 카페 1 + 체험 1 + 식사 1을 무작위 뽑아 붙인 구조
- 동선(動線) 없음 — 연남동 카페 → 청담 식당 같은 조합도 "코스"로 취급
- `_dateBlacklist`의 존재 = **데이터가 가짜라는 증거**. 진짜 코스였다면 블랙리스트가 필요 없음

---

## 한 줄 포지셔닝

> **"오늘 데이트 어디 갈까? — 진짜 다녀온 사람들의 코스만."**
> ODD는 코스를 *짓지 않습니다*. 검증된 코스를 *찾아드립니다*.

---

## 3가지 핵심 가치

1. **진짜 (Authentic)** — AI 창작 0%. 모든 코스에 출처와 "다녀온 흔적"
2. **동선 (Flow)** — 점이 아니라 선. 검증된 카페→체험→식사 이동을 판다
3. **즉답 (Instant)** — "어디 갈까"에서 "이거 가자"까지 30초

---

## 경쟁 지형과 ODD의 빈자리

| 앱 | 본질 | 약점 (= ODD의 기회) |
|---|---|---|
| 데이트팝 | 쿠폰·티켓 커머스 | 코스 동선 없음, "할인 상품" 중심 |
| 식신/망고플레이트 | 단일 맛집 평점 | "한 끼"는 알지만 "하루 동선" 모름 |
| 카카오맵 저장 | 내가 저장한 점(點)들 | 점만 있고 선(코스)이 없음 |
| 인스타 장소탐색 | 사진 예쁨, 신뢰 광고에 오염 | 협찬 vs 진짜 구별 불가 |

**모든 경쟁자가 점(장소)을 다룬다. ODD만 선(검증된 동선)을 다룰 수 있다.**

---

## 데이터 수집 전략

### 소스 우선순위

| 소스 | 합법 수집 | 우선순위 |
|---|---|---|
| **네이버 블로그** | ◎ 검색 API 공식 제공 | **1순위** |
| 카카오 (다음 검색) | ◎ 검색 API | 2순위 |
| 유튜브 쇼츠 | △ 자막 API는 회색 | 3순위 |
| 인스타그램 | ✗ API 봉쇄, 크롤링 금지 | **보류** |
| 틱톡 | ✗ | 보류 |

### 법적 황금률

> ODD DB에 저장되는 것은 **"가공된 사실 + 출처 링크"**이지 **"원문"이 아니다.**
> 원문은 메모리상에서 추출용으로만 쓰고 버린다.

- ✅ 합법: 네이버/카카오 API 통한 메타데이터·요약·링크 수집
- ⚠️ 조건부: 본문에서 "사실 정보"(장소명·주소·동선·팁)만 추출, 원문 표현 미저장
- 🚫 금지: 인스타/틱톡 스크래핑, 본문 통째 복사, robots.txt 무시

### 3단 품질 게이트

**Gate 1 — 코스성 판별 (규칙 기반, 무료)**
- 장소 2곳 이상 + 이동 신호어: "다음으로", "이어서", "→", "1코스/2코스", "걸어서"
- 데이트 신호어: "데이트", "커플", "기념일", "남친/여친"
- 탈락률 ~70% (정상)

**Gate 2 — AI 추출·구조화 (LLM, 창작 금지 모드)**
- 시스템 프롬프트: "본문에 명시된 것만 추출. 없는 장소 창작 금지."
- 출력: `{장소명, 순서, 카테고리, 이동수단, 동선팁}` JSON

**Gate 3 — 실존 검증 (카카오/네이버 로컬 API)**
- 추출된 장소명 → 로컬 API로 실제 좌표·영업 여부 확인
- 이 단계에서 사원·보리밥집·관공서 자동 차단 (블랙리스트 불필요)

### 스케일 목표 (양보다 신뢰)

| 시점 | 검증 코스 | 커버리지 |
|---|---|---|
| 2개월 | 500 | 서울 5개 핫플 (홍대·성수·강남·한남·종로) |
| 4개월 | 1,500 | 서울 전역 + 부산·대전 |
| 6개월 | 3,000~4,000 | 6대 광역시 (도시당 300개+) |

---

## AI 역할 재설계

### Before (현재 — 망가진 구조)
```
사용자 요청
→ Gemini: 코스 "생성" (장소를 지어냄)
→ _dateBlacklist 출력 필터 (증상 봉합)
→ 신뢰 안 가는 코스
```

### After (목표 — AI는 사서)
```
사용자: "강남, 감성, 저녁, 기념일"
→ [AI] 의도 이해 → 쿼리 벡터 인코딩
→ [pgvector] 진짜 코스 DB에서 의미적 K개 검색 (창작 0%)
→ [AI] 랭킹: 의미유사도 + 저장수 + 시간대 + 평점
→ 검증된 코스 Top 3 (출처 링크 포함)
```

### Supabase pgvector 활용
- `curated_courses`에 `embedding vector(1536)` 컬럼 추가
- 임베딩 텍스트: `제목 + 아키타입 + 동네 + 장소3개 + 동선팁`
- Edge Function `match_courses(city, mood, query)` — 클라이언트에 API 키 미노출

### 랭킹 SQL 골격
```sql
select *,
  (1 - (embedding <=> :query_vec)) * 0.5
  + least(save_count / 100.0, 1) * 0.2
  + time_slot_match(:slot) * 0.2
  + (rating / 5.0) * 0.1
  as match_score
from curated_courses
where city = :city and is_active
order by match_score desc limit 3;
```

---

## 6개월 실행 로드맵

### Phase 1 — 기반 전환 (1~2개월)

| 주 | 작업 | 검증 |
|---|---|---|
| W1 | 네이버 검색 API 발급, DATA_POLICY.md 작성, DB 스키마 설계 | API 호출 성공 |
| W2 | `curated_courses`에 source_url, embedding, is_verified 컬럼 + pgvector 활성화 | 마이그레이션 완료 |
| W3 | 수집기 v1: 네이버 API → Gate1 규칙 필터 | 코스성 통과율 측정 |
| W4 | Gate2 LLM 추출 + Gate3 카카오 검증 | 홍대 코스 50개 DB 적재 |
| W5 | 임베딩 파이프라인 + Edge Function match_courses 초안 | 의미 매칭 Top3 반환 |
| W6 | 앱 연동: Gemini 폴백 제거, Edge Function으로 교체 | 블랙리스트 라인 제거 가능 |
| W7 | 5개 핫플 코스 500개 확보 | 도시별 100개 |
| W8 | UI: 코스 카드에 "출처·작성일" 배지, 구 조합 데이터 is_active=false | 베타 피드백 |

**Phase 1 성공지표:** 검증 코스 500개, Gemini 의존 0%

### Phase 2 — 매칭 고도화 (3~4개월)
- 랭킹 가중치 튜닝, A/B 테스트
- 서울 전역 + 부산·대전 확장
- 자동 배치 cron (주 1회)
- 자연어 검색 ("비 오는 날 실내 강남")

**성공지표:** 검증 코스 1,500개, 코스 저장 전환율, 재방문율 상승

### Phase 3 — UGC (5~6개월)
- "내 코스 등록" 기능 (사용자 직접 추가)
- 커플 공동 저장·"다녀왔어요" 인증
- UGC도 3-게이트 자동 검수

**성공지표:** UGC 코스 20%+, WAU 상승

### Phase 4 — 수익화 (6개월+)
- 지역 상권 제휴 (코스 내 매장 우선 노출·예약)
- 협찬 코스는 반드시 "광고" 배지 분리 (신뢰 자산 보호)
- 프리미엄 (광고 제거·고급 코스)

---

## 다음 주 월요일에 할 3가지

### 1. 네이버 검색 API 키 발급
https://developers.naver.com/main/ → "검색" 앱 등록 → 블로그 검색 API
카카오 로컬 API 키도 같이 (https://developers.kakao.com)
소요 시간: 30분

### 2. DATA_POLICY.md 작성
`pipeline/DATA_POLICY.md` 한 페이지:
> "ODD는 블로그 원문을 저장·재배포하지 않으며,
> 추출된 사실 정보와 출처 링크만 보관한다."

### 3. 홍대 50개 end-to-end 파이프라인 완성
전국 만 개 X → 홍대 50개 → 네이버 API → 3게이트 → DB → 앱 표시
이 50개가 "진짜처럼" 보이면, 나머지는 스케일링 문제

---

## 핵심 철학

> AI의 자리를 "작가"에서 "사서(librarian)"로 옮긴다.
> 도서관에 가짜 책을 쓰게 하지 말고, 진짜 책을 잘 찾아주게 한다.
> AI의 창작(generation)은 독(毒), AI의 이해(embedding)와 정렬(ranking)은 약(藥).
